import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

import Navigation from './Navigation' // Importez le fichier de navigation

export default function App() {
  return <Navigation />; // Utilisez ici le composant Navigation
}; 